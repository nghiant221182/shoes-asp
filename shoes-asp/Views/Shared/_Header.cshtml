<nav class="colorlib-nav" role="navigation">
    <div class="top-menu">
        <div class="container">

            <div class="row">
                <div class="col-sm-7 col-md-9">
                    <div id="colorlib-logo">
                        <a asp-controller="Home" asp-action="Index">Cửa hàng giày</a>
                    </div>
                </div>

                <div class="col-sm-5 col-md-3">
                    <form asp-controller="Product" asp-action="Index" method="get" class="search-wrap">
                        <div class="form-group">
                            <input type="search" name="keyword"
                                   class="form-control search"
                                   placeholder="Tìm kiếm">
                            <button class="btn btn-primary submit-search text-center" type="submit">
                                <i class="icon-search"></i>
                            </button>
                        </div>
                    </form>
                </div>
            </div>


            <div class="row">
                @{
                    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
                    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
                }
                <div class="col-sm-12 text-left menu-1">
                    <ul>
                        <li class="@(currentController == "Home" && currentAction == "Index" ? "active" : "")">
                            <a asp-controller="Home" asp-action="Index">Trang chủ</a>
                        </li>

                        <li class="@(currentController == "Product" && currentAction == "Index" ? "active" : "")">
                            <a asp-controller="Product" asp-action="Index">Sản phẩm</a>
                        </li>

                        <li class="@(currentController == "Product" && currentAction == "Women" ? "active" : "")">
                            <a asp-controller="Product" asp-action="Women">Giày nữ</a>
                        </li>

                        <li class="@(currentController == "Home" && currentAction == "About" ? "active" : "")">
                            <a asp-controller="Home" asp-action="About">Giới thiệu</a>
                        </li>

                        <li class="@(currentController == "Home" && currentAction == "Contact" ? "active" : "")">
                            <a asp-controller="Home" asp-action="Contact">Liên hệ</a>
                        </li>

                        <li class="cart @(currentController == "Cart" && currentAction == "Index" ? "active" : "")">
                            <a asp-controller="Cart" asp-action="Index">
                                <i class="icon-shopping-cart"></i>
                                Giỏ hàng [0]
                            </a>
                        </li>
                    </ul>
                </div>
            </div>

        </div>
    </div>



    <div class="sale">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 offset-sm-2 text-center">

                    <div class="row">
                        <div class="owl-carousel2">

                            <div class="item">
                                <div class="col">
                                    <h3>
                                        <a href="#">
                                            Giảm giá 25% cho tất cả sản phẩm – Mã: SALE25
                                        </a>
                                    </h3>
                                </div>
                            </div>

                            <div class="item">
                                <div class="col">
                                    <h3>
                                        <a href="#">
                                            Khuyến mãi lớn – Giảm đến 50% giày mùa hè
                                        </a>
                                    </h3>
                                </div>
                            </div>

                        </div>
                    </div>

                </div>
            </div>
        </div>
    </div>

</nav>